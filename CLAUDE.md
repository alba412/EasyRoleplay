# プロジェクト方針

日本語ネイティブのAIロールプレイクライアント兼キャラクターカード投稿基盤。

英語圏のキャラクターカード資産は翻訳の手間・不自然さ・感性の差により日本人には扱いにくい。
翻訳で解決するのではなく、日本語で書かれたカードが集まる場所を作ることが目的。

## 開発方針

- まず個人で使えるレベルを目指す。将来的な公開を見据えるが、初版では実装しない
- 動くものを最短で出してから太らせる。全部を先に設計しない
- 1〜2機能ごとに動作確認する。まとめて作らない
- 動いた時点で必ずgit commit

## 技術構成

- Next.js (App Router) + TypeScript
- SQLite + Prisma（公開時にPostgresへ移行。それ以外は書き換えない）
- Tailwind CSS + shadcn/ui
- Zod（カードスキーマ定義・検証）

ローカルでは `npm run dev` → `localhost:3000` で使う。
OpenRouterとComfyUIはNext.jsのサーバ側から叩く（CORS回避）。

## ディレクトリ構成

```
prisma/           DB定義（schema.prisma・マイグレーション）
src/
  app/            画面とAPIルート
  lib/
    card/         カードスキーマ定義・PNG読み書き
    prompt/       構造化データ → プロンプト（コンパイル層）
    llm/          OpenRouterアダプタ
    image/        ComfyUIアダプタ
```

`prisma/` はプロジェクトルートに置く。Prismaの既定位置なので、
schemaパスの明示設定もCLIの `--schema` 指定も不要になる。

`lib/prompt/` と `lib/image/` の独立を維持すること。
プロンプト生成ロジックの改善が既存カード全部に効く構造を壊さない。

## 設計上の原則

### 構造化データとプロンプトを分離する

投稿者には構造化フォームを書かせ、アプリが「構造化データ → プロンプト文字列」に
コンパイルする。自由記述のdescriptionをそのままLLMに投げる設計にしてはいけない。
それをやると、後から品質改善する手段が永久に失われる。

### コンパイル層は純粋関数にする

```ts
// lib/prompt/compile.ts
compilePrompt({ card, persona, settings, history, worldInfo, modelProfile })
  => { messages, prefill }
```

API通信もDB操作もしない。入力から出力を作るだけ。単体でテストできる状態を保つ。

### 空欄はプロンプトに出力しない

カードの任意フィールドが空なら、見出しごと出力しない。トークンを無駄にしない。

### フィールドを安易に増やさない

「書くのが面倒」が投稿基盤の最大の死因。
新しいフィールドを足す前に「カードごとに変わるか?」を問う。
変わらないなら全体設定に置く。

## カードスキーマ

### 共通

| フィールド | 備考 |
|---|---|
| id | UUID |
| mode | `character` \| `world` |
| name | 必須 |
| summary | 必須。一覧に出る1〜2行 |
| tags | 必須。pixiv系語彙。chubのタグ体系は輸入しない |
| safety | 必須。`safe`/`sensitive`/`nsfw`/`explicit` |
| avatar | PNG本体 |
| appearanceTags | 画像生成用。LLMには渡さない |
| artistTags | 画像生成用。`@`はアプリ側で自動付与 |

### character モード

- firstPerson — 一人称
- secondPerson — 二人称（ユーザーの呼び方）。単一値
- speechStyle — 口調（語尾・敬語レベル・方言）
- dialogueExamples — `{状況, 発話}` のペア配列
- personality — 性格
- background — 背景
- relationship — ユーザーとの関係・距離感
- scenario — 初期状況
- greetings — 初回メッセージ（配列。複数から選べる）
- ngBehaviors — NG行動（キャラ崩壊防止）

必須は共通4項目 + name のみ。他は空でも動くこと。

### world モード

キャラクターを用意せず、世界観設定だけで進行するGMモード。

- worldRules — 世界のルール
- initialSituation — 初期状況
- castMembers — `{name, 設定, appearanceTags}` の配列
- openingText — 導入文

## dialogueExamples の設計意図

英語カードの MES_EXAMPLE はセリフを羅列するだけで、
「いつどの口調になるか」が伝わらない。

状況ラベルを付けることで、条件付きの規則として読ませる。
プロンプトには `[状況]「発話」` の形で出力する。

```
[挨拶] 「……ども。今日も来たんだ、先輩」
[褒められた] 「はぁ? べつに、そんなんじゃないし。どうでもいいけど」
[心配している] 「先輩。……ちゃんと寝てますか」
```

このキャラは心配時だけ敬語になる。ラベルが無いとLLMは混在と解釈して混ぜ始める。
日本語RPで最も効くフィールドなので、書きやすさを最優先すること（状況は自由記述）。

## プロンプト組み立て順

```
[system]
  1. 役割定義      ← 全体設定
  2. キャラ定義    ← カード
  3. World Info    ← 発火したものだけ
  4. ペルソナ      ← ユーザー設定
  5. 出力ルール    ← 全体設定
[/system]
[messages] greeting → 会話履歴 [/messages]
[prefill]  ← モデルが対応していれば
```

出力ルールを末尾に置くこと。LLMは末尾の指示に最も強く従う。
口調が崩れる原因の大半は、出力ルールが冒頭にあって履歴に埋もれること。

出力ルールでは一人称を**再掲する**。2で書いていても履歴が伸びると流される。

## 全体設定（カードではなくアプリ側に持つ）

- 地の文/セリフの比率（3段階程度）
- 応答の長さ（短め/普通/長め）
- 地の文のスタイル（`*斜体*` / 素の文 / 「」の外）
- 使用モデル、temperature
- コンテキスト上限

## モデル別プロファイル

```ts
{
  supportsPrefill: boolean      // 書き出し強制が使えるか
  systemRole: 'system' | 'user'
  contextLimit: number
  extraSuffix?: string
}
```

主に使うのは DeepSeek V3/V4、Nemotron 3 Ultra。他の主要モデルもカバーする。
DeepSeekはプレフィルが効くので、地の文で始めさせる書き出しを渡すと形式が安定する。
DeepSeekは指示を反復すると崩れやすいので、システムプロンプトは短く構造的に。

## トークン計算

日本語は英語の2〜3倍消費する。tiktokenはモデル横断では当てにならない。
文字数 × 1.5 を概算とし、APIレスポンスの実測値で補正する。

## 日本語UIの必須要件

- **IME確定Enterで誤送信しない**。`e.nativeEvent.isComposing` を必ず見る。
  日本語アプリとしての第一印象がここで決まる

## 画像生成（Anima / ComfyUI）

ComfyUIはユーザー自身のものを使う。アプリからは提供しない。
接続先は `127.0.0.1:8188` を初期値とし、設定で変更可能にする。

発火は**手動ボタンのみ**。自動生成はしない。

### プロンプト構成

品質タグ + カードの外見タグ + LLMが生成したシーン記述（自然文）を結合する。
Animaはタグと自然文を任意の順序で混在できる。

外見はカード側から機械的に合成し、LLMにはシーン情報だけ（表情・ポーズ・場所・カメラ）を
書かせる。これをやらないと生成ごとに髪型や服装がブレて挿絵として使えない。

### Animaの仕様（2026年5月時点で確認済み）

- 2Bパラメータ、Cosmos-Predict2-2B派生、テキストエンコーダはQwen-3 0.6B
- ComfyUIネイティブ対応
- タグ順序: `[品質/meta/年/safety] [1girl等] [キャラ名] [作品名] [絵師] [一般タグ]`
- タグは小文字・スペース区切り（アンダースコアはscore_*タグのみ）
- 推奨positive prefix: `masterpiece, best quality, score_7, safe, `
- 推奨negative: `worst quality, low quality, score_1, score_2, score_3, artist name, blurry, jpeg artifacts, chromatic aberration`
- safetyタグ: `safe` / `sensitive` / `nsfw` / `explicit` ← カードのsafetyフィールドと直結
- **絵師タグは `@` 必須**。付けないと効果が非常に弱い。UI側で自動付与すること
- 短いプロンプトは意図しない内容が出やすい。自然文なら最低2文以上。
  LLMが短いシーン記述しか返さなかった場合のフォールバックを用意する
- Turbo版: CFG 1、8〜12ステップ。RP中の挿絵は待たせられないので実質これ一択
- 通常版: 30〜50ステップ、CFG 4-5、解像度512²〜1536²
- Aesthetic版は品質タグ不要（score_*はむしろ使わない）。モデル版に応じて出し分ける

### ライセンス

モデル本体は CircleStone Labs Non-Commercial License（非商用）。
ただし**生成画像（Outputs）には非商用制限がかからず商用利用可能**。

禁止されるのは、モデルをAPI背後でホストして課金する、収益化製品の一機能として使う等。
ユーザー自身のComfyUIを叩く構成なら問題ない。
将来サーバ側生成を有料提供する場合のみ別途ライセンスが必要。

## カードの保存形式

**PNGファイルを正とし、DBはインデックスのみ持つ。**

理由: 公開前でも他人とファイルで交換でき、DBが壊れても資産が残る。

Character Card V2 の「器」を借りる（PNGのtEXtチャンクにbase64 JSONを埋め込む）。
1枚の画像で完結してDMやアップローダで自然に流通する性質は捨てない。

独自スキーマは V2 が公式に用意している `extensions` フィールドに入れる。
仕様違反ではなく想定された使い方。他クライアントから最低限読める状態を維持することが、
コールドスタート問題への保険になる。

## 会話

- 1カード + 1ペルソナ + メッセージ列
- 会話は複数保存できる
- 再生成は上書き（分岐ツリーは実装しない）
- グループチャットは実装しない

## ペルソナ

name と簡易的なdescriptionのみ。複数保存・切替可能。
画像生成プロンプトには渡さない。

## 実装順序

1. チャットが動く（OpenRouter、IME処理、キャラは決め打ち）
2. カードスキーマをZodで定義（Prismaのテーブル定義とTS型を揃える）
3. コンパイル層（カード → プロンプト）
4. カード管理UI（作成・編集・PNG入出力）
5. World Info（`{keys, content, priority, alwaysOn}` の配列）
6. ComfyUI連携
7. 投稿機能（認証・アップロード・タグ検索）。ここでPostgresへ移行

1〜3で「取り敢えず使える」状態になる。

## 実装しないと決めたもの

- 外見の状態遷移追跡（LLMのJSON出力が信用できないため）
- グループチャット
- 会話の分岐ツリー
- 画像の自動生成
- Character Card V3 / CHARX 対応（当面）

## 公開時の検討事項（初版では実装しない）

- **ゾーニングは後付けが効かない**ので、年齢制限フラグとタグは最初から入れる（済: safety）
- R-18を扱う場合、インフラ事業者の規約を先に確認する。
  Cloudflare Images/Streamはアダルトコンテンツ禁止。国内VPS等を検討
- 収益化は困難。広告は付かず、個人でのR-18サブスク決済は決済の壁でほぼ不可能。
  費用を月数千円に固定し、Ci-en/Fantia等の支援で相殺する前提で設計する
  （個人が直接課金すると特商法で氏名・住所の公開義務が生じる）
- 生成画像はサーバに保存しない。カードのファイルサイズ上限を設ける
- ストレージはS3互換APIで抽象化し、移行しやすくしておく
- 投稿ガイドライン: 既存作品の設定をそのまま載せたカードの扱いを決めておく
- SillyTavernはAGPL-3.0。コードを貼り付けるとライセンスが伝播する。
  仕様（カード形式）は互換にしつつ実装は独立させる
